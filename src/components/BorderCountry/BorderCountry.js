import React from 'react';

const BorderCountry = ({name}) => {
    return (
        <>
            <ul>
                <li>{name}</li>
            </ul>
        </>
    );
};

export default BorderCountry;