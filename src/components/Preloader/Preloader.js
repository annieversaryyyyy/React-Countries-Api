import React from 'react';
import './Preloader.css';

const Preloader = () => {
    return (
        <div>
            <div className="loader hide" >Loading...</div>
        </div>
    );
};

export default Preloader;